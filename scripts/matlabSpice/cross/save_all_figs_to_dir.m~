function save_all_figs_to_dir(dir_name)
figlist=findobj('type','figure');
    for i=1:numel(figlist)
    figlist(i);
    filename = strcat('fig',num2str(i));
    saveas(figlist(i),filename,'fig');
    saveas(figlist(i),fullbndir_name, filename,'jpg');
    end
end